# 历史记录

本文件保存已完成工作的证据、资源迁移背景和被替代方案。当前状态只看同目录的 `核心规则与当前任务.md`；历史需要追溯时正常查阅。

## 2026-08-29 文档分层迁移

- 将原核心文档中的尺寸基线和资源现状移入历史，核心文档仅保留规则、当前任务和门禁。

## 2026-08-26 核心建筑 5×5 与原生纯色预览

- 用户要求核心建筑在编辑器和实机统一改为 `5×5`。
- 用户要求取消禁止部署区域图片预览，改用 LDtk 原生纯色。
- 用户补充要求保留 `Building44`，因此采用新增 `Building55` 并迁移默认核心实例的方案。
- 当时基线：31 个核心建筑 Prefab 的碰撞体 X/Z 与模型直径均为 4，模型底面为 Y=0；LDtk 已有 `Building22/33/44`，尺寸为 32/48/64 px，格尺寸为 16 px。
- 迁移约束：既有 `Building44` 实例锚点在 16 px 整数倍；迁移到奇数格 `Building55` 时向右/下各移动 8 px，使 Unity 中心落在 TWC 格心。
- 旧预览图和 tileset 定义只被建筑实体预览引用，没有地形或其他实体消费者。
