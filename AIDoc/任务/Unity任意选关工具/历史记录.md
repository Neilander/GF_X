# Unity 任意选关工具：历史记录

本文件保存入口演进和已废弃的外部操作方案。当前状态只看同目录的 `核心规则与当前任务.md`；历史需要追溯时正常查阅。

## 2026-08-29 文档分层迁移

- 将原核心文档中的入口细节移入历史，核心文档仅保留不可变入口规则和当前验收。

## 2026-08-26

- 根因溯源确认：此前的窗口前台切换与坐标点击是 MCP 断联后的临时外部 fallback，不属于项目正式链。
- 项目内多个诊断 runner 已统一依赖 `EditorRuntimeLevelEntry.TryEnterWithDefaultCareer`，但各自重复实现 Launch、Play 和等待 procedure 的壳。
- 人工入口为 Unity 菜单 `Tools/Diagnostics/Enter Runtime Level...`；自动入口为 `EditorRuntimeLevelEntryLauncher.Enter("<LevelIdentifier>")`。
- 成功状态必须满足 `IsEntryPending == false` 且 `LastCompletedLevelIdentifier` 与目标一致。
