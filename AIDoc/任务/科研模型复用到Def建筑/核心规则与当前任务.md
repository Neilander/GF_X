# 核心规则与当前任务

- 目标：将旧 `BuilType.Tech` 中仍为非占位的科研建筑模型复用到同 `Archetype` 的 `BuilType.Def` 建筑。
- 当前确认的非占位 Tech Lv1 与 Def 映射：
  - `ServerRoom`（Coding）-> `DDoSDevice`
  - `GiantMascot`（Sightseeing）-> `ThemeStatue`
  - `NavStation`（Delivery）-> `ComplaintsDepartment`
  - `QualityCheck`（Butchery）-> `MeatRack`
- 旧 Tech 其余模型及 Lv2/Lv3 均为占位，不作为来源。
- 只替换 Def Prefab 的 `Display` 模型引用及对应 `EntityPresentationBindings.placeholder`；保留 Def 的逻辑脚本、武器、碰撞体、占地和 `PresentationSockets`。
- 模型来源在 `Archive/Buildings/2026-08-13_PreBuildingRework`，恢复到 `Assets/AAAGame/Models` 时保留原 `.meta` GUID。
- 当前阶段：准备恢复四个模型及 `QualityCheck` 所需的肉场材质/Prefab 依赖，然后更新四组 Def 的 Lv1-Lv3 显示引用。
