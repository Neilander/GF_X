# 历史记录

- 2026-09-01：将售货机 FBX 挂入 `Buil_VendingMachine_Lv1-Lv3`，统一设置模型实例缩放为 140、局部 Y 坐标为 1.33。
- 2026-09-01：复核三个 prefab 的最终包围盒约为 `0.86 × 1.78 × 0.86` 米，底部贴地，`placeholder=False`，Unity 控制台无新增错误。
- 2026-09-01：确认 FBX 内嵌材质未绑定贴图，创建 `VendingMachine_AI.mat` 并将底色贴图绑定到 `_BaseMap`。
- 2026-09-01：确认 FBX 根节点自带 `-90° X` 导入旋转，实例改为 `X=90°、Y=-90°、Z=0°`，模型立起并将商品面转向镜头左前。
- 2026-09-02：复核 `FbxToPrefab` 自动尺寸算法；之前 140 的实例缩放受旧 `Display=0.6667` 影响，视觉 XZ 只有约 `1.33`。现按最终世界包围盒将实例缩放调整为约 `325.04`，视觉 XZ 为 `2.00×1.99`，与建筑碰撞体 `2×2` 对齐。
- 2026-09-02：商品面从右前改为左前，实例旋转改为 `X=90°、Y=90°、Z=0°`；模型底部和中心已重新对齐。
- 2026-09-02：根据实机观察修正售货机朝向，撤销错误的 Y 轴旋转，改为 `X=-90°（270°）、Y=0°、Z=0°`；上下方向反转并重新贴地，三个等级视觉包围盒约为 `1.99×4.12×2.00`。
- 2026-09-02：按实机确认将售货机 Y 轴改为 `180°`，最终旋转为 `X=-90°（270°）、Y=180°、Z=0°`；重新校准后仍保持 XZ `1.99×2.00`，底部误差小于 `0.000001`。
