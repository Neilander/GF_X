# 小地图自动水面：历史记录

本文件保存范围选择、失败方案和实际验证数据。当前状态只看同目录的 `核心规则与当前任务.md`；历史需要追溯时正常查阅。

## 2026-08-29 文档分层迁移

- 将原核心文档中的现象根因和 Lv2 验证数据移入本文件，核心文档仅保留边界规则与未完成验收。

## 2026-08-28

- 用户反馈自动水面实现后，小地图水面背景消失。
- 追踪确认自动水面位于 `LevelEnvironmentBackground`，而小地图仍只读取 TWC `Water` BlueprintLayer。
- 确认修复必须同时统一纹理范围与世界坐标映射，不能只更换背景色。
- 直接读取当前 Lv2 资源确认 TWC 为 `82 x 60`，环境水面 Mesh 边界为 `210 x 188`，偏移为 `(64, 64)`；先前运行记录中的 `87 x 77 / 215 x 205` 已不是当前资源状态。
- 首版运行验证得到 `210 x 188` 小地图纹理，但实机截图显示地形与玩家视野被严重缩小。用户纠正：完整水面不是小地图范围；`98 x 76` 仅是最小范围，剩余 UI 比例需要继续用水面和 Fog 填满。
- 修正后 Lv2 实机为 `136 x 76`，Terrain/Fog 同尺寸并完整覆盖 `16:9` UI；聚焦测试 `2/2`、相机/环境背景/小地图回归 `14/14` 通过。
- 最终世界范围为 `(-27.5,-8.5)..(108.5,67.5)`，小地图内容与容器约 `451.58×254.02`；布局变化通过 `RefreshLayout()` 事件重建，不逐帧轮询。
