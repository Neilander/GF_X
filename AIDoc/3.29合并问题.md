1. SoldierFactory.AddInitialBuffs 和 BuffTestProcedure.OnShowEntitySuccess 里都根据 index 添加了相同的 Buff，需要去重，统一到 SoldierFactory。
2. ClusterSpawnSystem.SpawnCluster 硬依赖 BuffTestProcedure.SpawnSoldier()，通用生成系统不应依赖特定测试流程，重构时改为直接调用 GF.Entity.ShowEntity 或传入回调。
3. SoldierFactory.ShowSoldier 里 ShowEntity 后同步 GetEntity 拿不到（异步），AddInitialBuffs 走不到，重构时需要改到 OnShowEntitySuccess 回调里调用。
